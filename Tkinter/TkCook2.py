import tkinter as tk
import requests
import urllib
import urllib.request
from bs4 import BeautifulSoup

#Creating Lables:
Label(root, text="").grid(row=0, sticky=0) #Params
#Sticky param uses North, West, South, and East
#Buttoms
Button(root, text="", command=,).grid(row=0, sticky=0)#Params